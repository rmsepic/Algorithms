from shortest_path_binary_matrix import shortestPathBinaryMatrix
from is_bipartite import isBipartite
from k_weakest_rows import kWeakestRows

from max_heap import MaxHeap

if __name__ == "__main__":
	heap = MaxHeap()
	heap.insert(40)
	heap.insert(50)
	heap.insert(100)
	heap.insert(10)
	print(heap.heap)
	
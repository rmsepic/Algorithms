from typing import List

def kWeakestRows(mat: List[List[int]], k: int) -> List[int]:
	pq = []
	#while len(ans) != k:
	x = len(mat) // 2

	for i in range(0, len(mat)):
		# Civilian
		if mat[i][x] == 0:
			heappush(pq, ())

	if len(ans) == k:

		
	print(ans)	

